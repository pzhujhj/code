

栈顶	  1           0     第四次调用之后

	      13          1     第三次调用之后 

	      132         13    第二次调用之后
	
栈底      1326        132   第一次调用之后 
	

	value的内容    quitient的内容


函数被调用时，它的变量空间创建在运行时的堆栈上，而之前调用函数的变量仍保存在堆栈上，它们都有自己的私有空间，互不干扰，所以每次递归调用的时候，都会在新的堆栈区创建一批变量
最后返回的时候，应该从栈顶一个个弹出，并且销毁堆栈中的变量
